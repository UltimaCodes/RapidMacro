<Application x:Class="Macrofy.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:Macrofy.App"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>

            <!-- Palette: Spotify-style near-black with floating panels; teal accent (ours). -->
            <Color x:Key="RmAccentColor">#2DD4BF</Color>
            <SolidColorBrush x:Key="RmAccentBrush" Color="{StaticResource RmAccentColor}" />
            <SolidColorBrush x:Key="RmAccentMutedBrush" Color="#262DD4BF" />
            <SolidColorBrush x:Key="RmWindowBrush" Color="#0A0A0B" />
            <SolidColorBrush x:Key="RmSidebarBrush" Color="#000000" />
            <SolidColorBrush x:Key="RmPanelBrush" Color="#121316" />
            <SolidColorBrush x:Key="RmCardBrush" Color="#1C1D22" />
            <SolidColorBrush x:Key="RmCardBorderBrush" Color="#2A2C33" />
            <SolidColorBrush x:Key="RmHoverBrush" Color="#14FFFFFF" />

            <!-- Left-rail navigation item (pill) -->
            <Style x:Key="NavItemStyle" TargetType="RadioButton">
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="Bd" Background="Transparent" CornerRadius="22"
                                    Margin="10,2" Padding="14,9">
                                <ContentPresenter VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="IsChecked" Value="False" />
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource RmHoverBrush}" />
                                </MultiTrigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource RmAccentMutedBrush}" />
                                    <Setter Property="Foreground" Value="{StaticResource RmAccentBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Device list card -->
            <Style x:Key="DeviceCardStyle" TargetType="ListBoxItem">
                <Setter Property="Margin" Value="0,0,0,8" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Border x:Name="Bd" Background="{StaticResource RmCardBrush}"
                                    BorderBrush="{StaticResource RmCardBorderBrush}" BorderThickness="1"
                                    CornerRadius="10" Padding="14,12">
                                <ContentPresenter />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="#3C434F" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource RmAccentBrush}" />
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource RmAccentMutedBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
